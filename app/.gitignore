# Dependencies — never committed, never shipped (the Apps container runs its own npm install)
node_modules/

# Build outputs — regenerated by `npm run build:source` / scripts/build-app.sh.
# Gitignored here, but force-shipped to the deployed app via databricks.yml `sync.include`.
dist/
client/dist/
drizzle/

# Local dev
.env
.env.*
!.env.template
!.env.example
*.bak
.preview.pgid
.smoke-test/
playwright-report/
test-results/
.databricks/
